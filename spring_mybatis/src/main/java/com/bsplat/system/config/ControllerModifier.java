package com.bsplat.system.config;


public class ControllerModifier {
	
	/*public void addGlobalVars(Object retVal) {
		if (retVal instanceof ModelAndView) {
			ModelAndView mv = (ModelAndView)retVal;
			mv.addObject("context", BsPlatContext.getContext());
		}
	}
*/
}
